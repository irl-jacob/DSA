# Create an empty list
nums = []

# Add one element
nums.append(5)

# Loop through values
for num in nums:
    print(num)

# Loop through indices
for i in range(len(nums)):
    print(nums[i])

# Return from a function
return nums